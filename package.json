{
  "name": "coriv",
  "version": "0.1.0",
  "private": true,
  "homepage": "http://corcc.github.io/coriv",
  "dependencies": {
    "react": "^16.4.2",
    "react-dom": "^16.4.2",
    "react-loading-icons": "^1.0.6",
    "react-twemoji": "0.3.0",
    "thousands": "^1.0.1"
  },
  "devDependencies": {
    "gh-pages": "^1.0.0",
    "react-scripts": "1.0.7"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "GENERATE_SOURCEMAP=false react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build -v **/*.map"
  }
}
